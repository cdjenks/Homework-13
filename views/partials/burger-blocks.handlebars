<li>
    {{burger_name}}
    {{#unless devoured}}
    <button class="burger-btn" data-id="{{id}}" data-devoured="{{devoured}}">Devour It</button>
    {{/unless}}
    {{#if devoured}}
    <button class="burger-btn" data-id="{{id}}" data-devoured="{{devoured}}">Want Again</button>
    {{/if}}
</li>